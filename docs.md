# 📚 Основы криптографии и ГОСТ

Этот документ предназначен для новичков и объясняет основные понятия, используемые в проекте.

## 🗺️ Общая схема (ASCII)

Ниже представлена упрощенная схема взаимодействия компонентов:

```text
┌─────────────────────────────────────────────────────────────────────────┐
│                          КРИПТОГРАФИЧЕСКИЙ КОНТЕЙНЕР                    │
│                                                                         │
│  ┌───────────────────┐               ┌──────────────────────────────┐   │
│  │   ЗАКРЫТЫЙ КЛЮЧ   │               │       ОТКРЫТЫЙ КЛЮЧ          │   │
│  │   (Private Key)   │──────────────▶│        (Public Key)          │   │
│  │      key.pem      │  (математ.    │                              │   │
│  └───────────────────┘   связь)      └──────────────┬───────────────┘   │
│            │                                        │                   │
│            │ (подписывает)                          │ (вкладывается в)  │
│            ▼                                        ▼                   │
│  ┌───────────────────┐               ┌──────────────────────────────┐   │
│  │ ЦИФРОВАЯ ПОДПИСЬ  │               │         СЕРТИФИКАТ           │   │
│  │    (Signature)    │               │        (Certificate)         │   │
│  └───────────────────┘               │          cert.pem            │   │
│                                      └──────────────┬───────────────┘   │
│                                                     │                   │
│                                                     │ (подписывается)   │
│                                                     ▼                   │
│                                      ┌──────────────────────────────┐   │
│                                      │     ЦЕПОЧКА СЕРТИФИКАТОВ     │   │
│                                      │      (Certificate Chain)     │   │
│                                      │  [Ваш] -> [УЦ] -> [Корневой] │   │
│                                      └──────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

## 🔑 Основные понятия

### 1. Закрытый ключ (Private Key)
*   **Что это:** Секретный файл (обычно `key.pem`).
*   **Зачем нужен:** Используется для создания **электронной подписи** и **расшифровки** данных, присланных вам.
*   **Важно:** Никогда и никому не передавайте этот файл! Если кто-то получит ваш закрытый ключ, он сможет подписывать документы от вашего имени.

### 2. Открытый ключ (Public Key)
*   **Что это:** Математический "парник" закрытого ключа.
*   **Зачем нужен:** Позволяет другим людям **проверять вашу подпись** и **зашифровывать** данные для вас.
*   **Важно:** Его можно (и нужно) передавать всем, с кем вы хотите общаться защищенно. Обычно он передается внутри **сертификата**.

### 3. Сертификат (Certificate)
*   **Что это:** "Паспорт" вашего открытого ключа (обычно `cert.pem` или `.crt`).
*   **Что внутри:**
    *   Сам открытый ключ.
    *   Информация о владельце (ФИО, организация, домен).
    *   Срок действия.
    *   **Подпись Удостоверяющего Центра (УЦ)**, которая подтверждает, что этот ключ действительно принадлежит вам.

### 4. Цепочка сертификатов (Certificate Chain)
Чтобы проверить, что ваш сертификат настоящий, нужно проверить подпись УЦ под ним. Чтобы проверить сертификат УЦ, нужна подпись вышестоящего УЦ. Так мы доходим до самого главного — **Корневого сертификата** (Root CA), которому мы доверяем априори.

### 5. Криптоконтейнер
В российской криптографии (КриптоПро и др.) под "контейнером" часто понимают папку или файл, где лежат:
*   Закрытый ключ.
*   Сертификат (с открытым ключом).
*   Служебная информация.

В OpenSSL мы чаще работаем с отдельными файлами `.pem`, но они составляют ту же логическую единицу.

---

## 🗂️ Форматы файлов

В мире криптографии одни и те же данные (ключи или сертификаты) могут быть упакованы по-разному. Вот основные форматы, с которыми вы столкнетесь:

### 1. PEM (Privacy Enhanced Mail)
*   **Расширения:** `.pem`, `.key`, `.crt`, `.cer`.
*   **Как выглядит:** Текстовый файл, который начинается с `-----BEGIN ...-----` и заканчивается `-----END ...-----`. Внутри — данные в кодировке Base64.
*   **Зачем нужен:** Самый популярный формат в Linux и OpenSSL. Его удобно открывать блокнотом или копировать в конфиги.

### 2. DER (Distinguished Encoding Rules)
*   **Расширения:** `.der`, `.crt`, `.cer`.
*   **Как выглядит:** Бинарный (двоичный) файл. Если открыть его в блокноте, увидите "кракозябры".
*   **Зачем нужен:** Часто используется в Java, Windows и для передачи сертификатов в некоторых сетевых протоколах.

### 3. PKCS#12 (PFX)
*   **Расширения:** `.p12`, `.pfx`.
*   **Как выглядит:** Бинарный файл, защищенный паролем.
*   **Зачем нужен:** "Все в одном". Внутри одного файла может лежать и **закрытый ключ**, и **сертификат**, и даже вся **цепочка УЦ**. Удобно для переноса ключей между системами (например, из Windows в Linux).
*   **Режимы работы (Legacy vs Modern):**
    *   **Стандартные PFX** (RSA/ECDSA): Работают в OpenSSL 3.x по умолчанию.
    *   **ГОСТ PFX** (созданные в CryptoPro): Часто используют старые алгоритмы шифрования (PBE с SHA1 и RC2-40), которые в OpenSSL 3.x считаются небезопасными. Для работы с ними **обязателен** флаг `-legacy`.

### 4. CSR (Certificate Signing Request)
*   **Расширения:** `.csr`.
*   **Что это:** Это не ключ и не сертификат, а **запрос** на получение сертификата. Вы отправляете этот файл в Удостоверяющий Центр (УЦ).

---

## 🔄 Полезные команды для конвертации

Если у вас есть файл в одном формате, а программа требует другой, используйте эти команды:

**Из PEM в DER:**
```bash
openssl x509 -in cert.pem -outform der -out cert.der
```

**Из DER в PEM:**
```bash
openssl x509 -in cert.der -inform der -out cert.pem
```

**Создание PFX (PKCS#12) из ключа и сертификата:**
```bash
openssl pkcs12 -export -out identity.p12 -inkey key.pem -in cert.pem -certfile chain.pem
```

**Извлечение PEM из PFX:**

1. **Современный/Стандартный PFX:**
```bash
# Извлечь всё без шифрования ключа
openssl pkcs12 -in identity.p12 -out identity.pem -nodes
```

2. **Legacy/ГОСТ PFX (CryptoPro):**
```bash
# Извлечение только сертификатов (работает с -legacy)
openssl pkcs12 -in identity.pfx -nokeys -out certs.pem -legacy

# Особенности закрытых ключей:
# Если PFX создан средствами CryptoPro с использованием проприетарных алгоритмов шифрования (например, GOST PBE OID: 1.2.840.113549.1.12.1.80), 
# OpenSSL не сможет извлечь закрытый ключ из-за отсутствия поддержки этого алгоритма.
# Это НЕ всегда означает, что ключ "неэкспортируемый", это ограничение совместимости форматов.
# В этом случае используйте оригинальную папку контейнера и get-cpcert.
```

---

## 🏛️ Инструкция по работе с ключами и сертификатами ГОСТ (ЕСИА)

В данном руководстве описаны процессы генерации собственных ключей по стандарту ГОСТ и обновления актуальных сертификатов ЕСИА.

### 1. Генерация новых тестовых ключей (ГОСТ)

Для имитации взаимодействия с ЕСИА на тестовом стенде необходимо создать пары ключей (закрытый ключ и самоподписанный сертификат) по стандарту ГОСТ 34.10-2012.

В нашем Docker-образе поддержка ГОСТ уже настроена.

```bash
# Генерация ключа и сертификата клиента (512 бит, paramset:A)
openssl req -x509 -outform pem -nodes -days 18250 -utf8 \
  -subj "/C=RU/CN=Test Client GOST" \
  -keyout client.gost.test.key \
  -out client.gost.test.cer \
  -newkey gost2012_512 -pkeyopt paramset:A
```

### 2. Просмотр содержимого и проверка

Для проверки структуры сертификата и его содержимого используйте:

```bash
# Просмотр ASN.1 структуры
openssl asn1parse -i -in client.gost.test.cer

# Просмотр текстовой информации о сертификате
openssl x509 -in client.gost.test.cer -text -noout
```

### 3. Извлечение публичных ключей

Для некоторых конфигураций может потребоваться публичный ключ отдельно от сертификата.

```bash
# Извлечение для сертификата
openssl x509 -noout -pubkey -in client.gost.test.cer -out client.gost.test.public.key
```

### 4. Обновление публичных ключей ЕСИА (Production)

Периодически ЕСИА обновляет свои сертификаты. Ниже приведены шаги для автоматизации их получения (только ГОСТ).

```bash
# 1. Скачивание актуального архива с портала ЕСИА
wget --no-check-certificate https://esia.gosuslugi.ru/public/esia.zip
# Распаковка с конвертацией кодировки имен файлов (ZIP обычно использует CP866)
# Для корректного отображения имен в терминале используйте zsh
7z x -mcp=866 esia.zip

# 2. Подготовка ГОСТ сертификата
# Конвертация из бинарного DER в текстовый PEM
# (Имя файла в архиве может меняться, например ГОСТ_PROD_25_26.cer)
openssl x509 -inform der -in "ГОСТ_PROD_XX.cer" -out esia.gost.prod.cer

# 3. Извлечение публичного ключа ГОСТ
openssl x509 -noout -pubkey -in esia.gost.prod.cer -out esia.gost.prod.public.key
```

### Полезные ссылки
* [Методические рекомендации ЕСИА](https://digital.gov.ru/documents/metodicheskie-rekomendaczii-po-ispolzovaniyu-esia)
* [Технологический портал ЕСИА](https://digital.gov.ru/ru/activity/govservices/infosystems/esia/)

---

## 🧪 Практические примеры проверки через Docker

Чтобы убедиться, что всё работает правильно, вы можете запустить эти команды прямо через Docker.

### Проверка генерации 512-битного ключа ГОСТ 2012

```bash
docker run --rm openssl-gost sh -c "
openssl req -x509 -outform pem -nodes -days 365 -utf8 \
  -subj '/C=RU/CN=Test ESIA' \
  -keyout test.key \
  -out test.cer \
  -newkey gost2012_512 -pkeyopt paramset:A && \
openssl x509 -in test.cer -text -noout | grep -E 'Public Key Algorithm|Signature Algorithm'
"
```

**Ожидаемый результат:**
```text
Signature Algorithm: GOST R 34.10-2012 with GOST R 34.11-2012 (512 bit)
Public Key Algorithm: GOST R 34.10-2012 with 512 bit modulus
```

---

### 🌐 Использование curl с ГОСТ

Поддержка ГОСТ в `curl` реализована через OpenSSL ENGINE.

**Проверка TLS-соединения с сайтом Госуслуг:**
```bash
docker compose run --rm openssl-gost curl -v -k https://www.gosuslugi.ru/
```
В выводе вы увидите строку вида:
`* SSL connection using TLSv1.2 / GOST2012-KUZNYECHIK-KUZNYECHIKOMAC`

**Запрос с использованием конкретного шифра:**
```bash
docker compose run --rm openssl-gost curl -k --ciphers GOST2012-GOST8912-GOST8912 https://rkntrust.ru/
```

---

## 🔒 Использование stunnel для ГОСТ-туннелей

`stunnel` позволяет создать локальный прокси, который будет терминировать или устанавливать ГОСТ-соединения.

**Пример конфигурации (`stunnel.conf`):**
```ini
[gost-proxy]
client = yes
accept = 0.0.0.0:4433
connect = gost.example.com:443
engine = gost
```

**Запуск stunnel через Docker:**
```bash
docker compose run --rm -v $(pwd)/stunnel.conf:/etc/stunnel/stunnel.conf openssl-gost stunnel /etc/stunnel/stunnel.conf
```

---

## 🛠️ Утилита get-cpcert

Утилита для извлечения сертификатов из контейнеров КриптоПро.

**Использование:**
```bash
docker compose run --rm -v /path/to/container:/container openssl-gost get-cpcert /container [password]
```

## 🛠️ Дополнительные инструменты

В образе предустановлены вспомогательные инструменты для профессиональной работы:

### 🧮 gostsum
Аналог `md5sum` или `sha256sum`, но для ГОСТ Р 34.11-2012.

```bash
docker compose run --rm openssl-gost gostsum file.txt
```

### 🐍 Python 3 + gostcrypto
Позволяет писать скрипты для автоматизации криптографических операций.

```python
# script.py
import gostcrypto
data = b"Hello"
hasher = gostcrypto.gosthash.new('streebog256')
hasher.update(data)
print(hasher.hexdigest())
```

Запуск:
```bash
docker compose run --rm -v $(pwd):/app openssl-gost python3 /app/script.py
```

### 🟢 Node.js + gostcrypto
Реализация алгоритмов ГОСТ на чистом JavaScript (WebCrypto API).

```javascript
// script.js
const engine = require('/usr/local/lib/node_modules/gostcrypto/lib/gostEngine.js');
const digest = engine.getGostDigest({name: 'GOST R 34.11', length: 256});
console.log(Buffer.from(digest.digest('Hello')).toString('hex'));
```

Запуск:
```bash
docker compose run --rm -v $(pwd):/app openssl-gost node /app/script.js
```

### 📦 jq
Для обработки JSON-ответов от API.

```bash
docker compose run --rm openssl-gost sh -c "echo '{\"status\": \"ok\"}' | jq .status"
```

# 📝 Подпись и проверка (CMS/CAdES)

Для работы с подписью можно использовать как стандартный `openssl cms`, так и предустановленные хелперы.

#### Использование хелперов (рекомендуется)
```bash
# Подпись
docker compose run --rm openssl-gost gost-cms-sign cert.pem key.pem file.txt

# Проверка
docker compose run --rm openssl-gost gost-cms-verify cert.pem file.txt.sig
```

#### Использование OpenSSL напрямую
```bash
# Подпись (CMS DER)
docker compose run --rm openssl-gost \
  openssl smime -sign -engine gost -binary \
  -signer cert.pem -inkey key.pem \
  -in file.txt -out file.txt.sig -outform DER

# Проверка
docker compose run --rm openssl-gost \
  openssl smime -verify -engine gost -inform DER -noverify \
  -certfile cert.pem -in file.txt.sig -content file.txt
```

---

## 🇷🇺 ГОСТ Алгоритмы простыми словами

*   **ГОСТ Р 34.10-2012**: Это стандарт для **подписи**. Если вам нужно "подписать документ", вы используете этот алгоритм. Он бывает двух видов: 256 бит (обычный) и 512 бит (усиленный).
*   **ГОСТ Р 34.11-2012 (Стрибог)**: Это стандарт для **хеширования**. Он превращает любой файл в уникальную "строку-отпечаток" фиксированной длины. Если в файле изменится хотя бы один бит, хеш станет совсем другим.
*   **ГОСТ Р 34.12-2015 (Кузнечик / Магма)**: Это стандарты **шифрования**. Используются для того, чтобы никто не смог прочитать содержимое файла без ключа.

---

## 🛠 Как это работает в этом проекте

Этот Docker-образ содержит инструменты (`openssl`, `stunnel`, `curl`), которые "научены" понимать эти российские алгоритмы. Без специального "движка" (GOST Engine), обычный OpenSSL не знает, как работать с ГОСТ-ключами.

**Пример жизненного цикла:**
1.  Вы генерируете **Закрытый ключ** (ГОСТ 2012).
2.  На его основе создаете **Запрос на сертификат** (CSR).
3.  Запрос отправляется в УЦ, который выдает вам **Сертификат**.
4.  Теперь вы можете использовать эту пару для настройки защищенного сайта или подписи документов.
